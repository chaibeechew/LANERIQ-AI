name: RETIRED - Legacy 18-Page Product Surface Gate

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy gate retired
        run: |
          echo "The fixed 18-page product-surface gate is retired and is no longer a Canonical UI or Production release authority."
          echo "Use LANERIQ Canonical UI Regression Gate instead."
