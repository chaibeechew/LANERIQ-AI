function n(v,f=Infinity){const x=Number(v);return Number.isFinite(x)?x:f;}function clean(v,max=120){return String(v||"").trim().slice(0,max);}export const AVATAR_QUALITY_CLOSURE_V1="laneriq-avatar-quality-closure-v1";
const DEFAULTS=Object.freeze({minHighTierFps:55,minMidTierFps:28,maxFrameP95Ms:40,maxVoiceStartP50Ms:500,maxVoiceStartP95Ms:800,maxLipSyncP95Ms:80,maxBargeInP95Ms:150,maxIdentityDrift:.08,minGestureIntentAccuracy:.9,maxFootSlipRate:.02,maxCollisionRate:.01,minEmotionCoherence:.9,minEyeContactScore:.85,maxCrashRate:.003,maxThermalSeriousRate:.02,maxBatteryDrainPctHour:15,maxPeakMemoryMb:380,minRecoveryRate:.997,minContinuityRate:.997});
export function getAvatarQualityClosureThresholds(overrides={}){return{...DEFAULTS,...overrides};}
export function evaluateAvatarQualityClosure(metrics={},thresholds=getAvatarQualityClosureThresholds()){
 const checks={highTierFps:n(metrics.highTierFps,0)>=thresholds.minHighTierFps,midTierFps:n(metrics.midTierFps,0)>=thresholds.minMidTierFps,frameP95:n(metrics.frameP95Ms)<=thresholds.maxFrameP95Ms,voiceP50:n(metrics.voiceStartP50Ms)<=thresholds.maxVoiceStartP50Ms,voiceP95:n(metrics.voiceStartP95Ms)<=thresholds.maxVoiceStartP95Ms,lipSync:n(metrics.lipSyncP95Ms)<=thresholds.maxLipSyncP95Ms,bargeIn:n(metrics.bargeInP95Ms)<=thresholds.maxBargeInP95Ms,identity:n(metrics.identityDrift,1)<=thresholds.maxIdentityDrift,gesture:n(metrics.gestureIntentAccuracy,0)>=thresholds.minGestureIntentAccuracy,footSlip:n(metrics.footSlipRate,1)<=thresholds.maxFootSlipRate,collision:n(metrics.collisionRate,1)<=thresholds.maxCollisionRate,emotion:n(metrics.emotionCoherence,0)>=thresholds.minEmotionCoherence,eyeContact:n(metrics.eyeContactScore,0)>=thresholds.minEyeContactScore,crash:n(metrics.crashRate,1)<=thresholds.maxCrashRate,thermal:n(metrics.thermalSeriousRate,1)<=thresholds.maxThermalSeriousRate,battery:n(metrics.batteryDrainPctHour)<=thresholds.maxBatteryDrainPctHour,memory:n(metrics.peakMemoryMb)<=thresholds.maxPeakMemoryMb,recovery:n(metrics.recoveryRate,0)>=thresholds.minRecoveryRate,continuity:n(metrics.continuityRate,0)>=thresholds.minContinuityRate};const failed=Object.entries(checks).filter(([,ok])=>!ok).map(([k])=>k);return{contract:AVATAR_QUALITY_CLOSURE_V1,pass:failed.length===0,score:Math.round((Object.values(checks).filter(Boolean).length/Object.keys(checks).length)*100),checks,failed,thresholds,benchmarkLabel:clean(metrics.benchmarkLabel||"avatar-commercial-v1"),requiresPhysicalEvidence:true,requiresHumanPerceptionPanel:true,automaticPromotion:false};}
export function getAvatarQualityClosureReadiness(){return{contract:AVATAR_QUALITY_CLOSURE_V1,latencyGate:true,lipSync80msGate:true,bargeIn150msGate:true,identityDriftGate:true,gestureQualityGate:true,emotionEyeContactGate:true,thermalBatteryMemoryGate:true,recoveryContinuityGate:true,codeReady:true,commercialBenchmarkPassed:false};}
